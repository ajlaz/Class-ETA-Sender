/*
 * Project Idea: Create a scheduled job that uses google calendar information, dorm information, and google maps information to send a text
 * at approximately 9am on weekdays to calculate the commute to my first class
 */

/**
 * @param {Date} date current date to use for calendar 
 * @param {string} id calendar id for request
 * @returns {string} json response with first class on calendar
 */
function getFirstCalendarEvent(date, id) {
/**
 * TODO: Load google calendar events from HTTP request: GET https://www.googleapis.com/calendar/v3/calendars/${calendarId}/events
 * TODO: Parse json response of calendar to return the first date.
 * DOC REFERENCE: https://developers.google.com/calendar/api/quickstart/nodejs

 */
}